

<form method="post" action="{{ path('app_claim_delete', {'id': claim.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="delete-button">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ claim.id) }}">
    <button type="submit" class="btn-icon" aria-label="Delete">
        <i class="fas fa-trash-alt"></i> <!-- Icône de la poubelle -->
    </button>
</form>
