<form method="post" action="{{ path('app_formquestions_delete', {'id': formquestion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formquestion.id) }}">
    <button class="btn">Delete</button>
</form>
