{% extends 'base.html.twig' %}

{% block title %}TaskEvent{% endblock %}

{% block body %}
    <h1>TaskEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task_event.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ task_event.Description }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ task_event.Etat }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ task_event.CreationDate ? task_event.CreationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedDate</th>
                <td>{{ task_event.UpdatedDate ? task_event.UpdatedDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_task_event_index') }}">back to list</a>

    <a href="{{ path('app_task_event_edit', {'id': task_event.id}) }}">edit</a>

    {{ include('task_event/_delete_form.html.twig') }}
{% endblock %}
