<form method="post" action="{{ path('app_task_event_delete', {'id': taskEvent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taskEvent.id) }}">
    <button class="btn">Delete</button>
</form>
